{
  "name": "obsidian-chat-bridge",
  "version": "1.6.0",
  "description": "Browser extension that connects AI chat interfaces to an Obsidian vault through Local REST API.",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "watch": "node ./scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.331",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2"
  }
}
